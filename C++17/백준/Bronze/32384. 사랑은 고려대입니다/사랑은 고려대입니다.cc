#include "bits/stdc++.h"
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s = "LoveisKoreaUniversity";

    int c; cin >> c;

    for (int i = 0; i < c; ++i)
    {
        cout << s << " ";
    }
    
    
    return 0;
}
