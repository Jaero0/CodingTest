#include "bits/stdc++.h"
using namespace std;

int main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int c; cin >> c;

    for (int i = 1; i <= c; ++i)
    {
        cout << i << "\n";
    }
    
    return 0;
}
